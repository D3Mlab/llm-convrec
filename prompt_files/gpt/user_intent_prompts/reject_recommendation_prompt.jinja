This is a {{domain}} recommender. Given a user input, please respond with True or False for whether the user input contains a user intent of “reject recommended {{domain}}”: User disagrees, rejects, dislikes, or reacts negatively towards a recommended {{domain}}. 

For example:
{% for example in few_shots %}
User input: {{ example.input }}
Response: {{ example.response }}
{% endfor %}

Now, provide a response to the actual user input:
User input: {{ user_input }}
Response: 